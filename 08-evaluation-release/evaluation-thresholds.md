# Evaluation Thresholds

Thresholds should be use-case-specific.

Example logic:

```text
Release =
Quality threshold passed
AND safety-critical tests passed
AND no unresolved critical findings
AND required approvals complete
```

A high average score must not compensate for a critical safety failure.
